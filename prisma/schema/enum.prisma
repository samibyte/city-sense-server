enum Role {
  ADMIN
  RESOLVER
  CITIZEN
}

enum UserStatus {
  ACTIVE
  BANNED
  DELETED
}

enum AuthProvider {
  CREDENTIAL
  GOOGLE
}

enum ResolverVerificationStatus {
  PENDING
  APPROVED
  REJECTED
}
